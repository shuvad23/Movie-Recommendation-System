[build]
  # Command to build your app
  command = "npm run build"
  # Folder to publish (React build folder)
  publish = "build"

# Redirect all routes to index.html for React Router SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200